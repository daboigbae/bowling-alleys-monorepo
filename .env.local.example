# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Email (Resend - for auth verification codes)
# Get API key from resend.com/api-keys
# For testing use onboarding@resend.dev; for production use your verified domain (e.g. noreply@bowlingalleys.io)
USE_PRODUCTION_EMAIL=false
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev

# Firebase Admin SDK (JSON string - same for local & Railway)
# Get from Firebase Console → Project Settings → Service Accounts → Generate new private key
# Minify the JSON to a single line, e.g. FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account",...}'
FIREBASE_SERVICE_ACCOUNT_KEY=

# Firebase Client SDK Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id_here

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000

# Instructions to set up Firebase:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new Firebase project
# 3. Click "Add app" and select Web platform (</>)
# 4. Go to Authentication > Sign-in method and enable:
#    - Email/Password
#    - Google (configure OAuth consent screen)
# 5. Go to Firestore Database and create database in production mode
# 6. Set up Firestore security rules (see Firebase console)
# 7. Add localhost and your Railway deployment URL to Authentication > Settings > Authorized domains
# 8. Copy the config values from Project Settings > SDK setup and configuration
# 9. Run the seed script: tsx scripts/seed.ts (after setting up environment variables)
